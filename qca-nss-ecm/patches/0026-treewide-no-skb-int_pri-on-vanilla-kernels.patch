Read no skb int_pri on kernels without the QSDK QoS patches

skb->int_pri is an sk_buff field that only exists with the QSDK
kernel patchset, where it carries an internal priority from the
WiFi driver into the PPE/SFE QoS offload rules. This tree runs on
vanilla OpenWrt kernels which do not have the field, and its only
consumers (the PPE and SFE rule QoS fields) do not apply to the NSS
front-end, so read it as 0.

diff -Nur '--exclude=.git' ecm-final-base/ecm_classifier_dscp.c ecm-final/ecm_classifier_dscp.c
--- a/ecm_classifier_dscp.c
+++ b/ecm_classifier_dscp.c
@@ -215,13 +215,13 @@
 {
 	if (sender == ECM_TRACKER_SENDER_TYPE_SRC) {
 		cdscpi->process_response.flow_qos_tag = skb->priority;
-		cdscpi->process_response.flow_int_pri = skb->int_pri;
+		cdscpi->process_response.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 		cdscpi->process_response.flow_mark = skb->mark;
 		cdscpi->process_response.flow_dscp = ip_hdr->ds >> XT_DSCP_SHIFT;
 		cdscpi->packet_seen[ECM_CONN_DIR_FLOW] = true;
 	} else {
 		cdscpi->process_response.return_qos_tag = skb->priority;
-		cdscpi->process_response.return_int_pri = skb->int_pri;
+		cdscpi->process_response.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 		cdscpi->process_response.return_mark = skb->mark;
 		cdscpi->process_response.return_dscp = ip_hdr->ds >> XT_DSCP_SHIFT;
 		cdscpi->packet_seen[ECM_CONN_DIR_RETURN] = true;
@@ -506,7 +506,7 @@
 		 */
 		if (sender == ECM_TRACKER_SENDER_TYPE_SRC) {
 			cdscpi->process_response.flow_qos_tag = skb->priority;
-			cdscpi->process_response.flow_int_pri = skb->int_pri;
+			cdscpi->process_response.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			cdscpi->process_response.flow_mark = skb->mark;
 			cdscpi->process_response.flow_dscp = ip_hdr->ds >> XT_DSCP_SHIFT;
 
@@ -516,7 +516,7 @@
 			 * the rule to NSS. So, let's update them, if they are not set.
 			 */
 			if (cdscpi->process_response.return_int_pri == 0) {
-				cdscpi->process_response.return_int_pri = skb->int_pri;
+				cdscpi->process_response.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			}
 
 			if (cdscpi->process_response.return_qos_tag == 0) {
@@ -533,7 +533,7 @@
 
 		} else {
 			cdscpi->process_response.return_qos_tag = skb->priority;
-			cdscpi->process_response.return_int_pri = skb->int_pri;
+			cdscpi->process_response.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			cdscpi->process_response.return_mark = skb->mark;
 			cdscpi->process_response.return_dscp = ip_hdr->ds >> XT_DSCP_SHIFT;
 
@@ -544,7 +544,7 @@
 			 */
 
 			if (cdscpi->process_response.flow_int_pri == 0) {
-				cdscpi->process_response.flow_int_pri = skb->int_pri;
+				cdscpi->process_response.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			}
 
 			if (cdscpi->process_response.flow_qos_tag == 0) {
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_multicast_ipv4.c ecm-final/frontends/cmn/ecm_multicast_ipv4.c
--- a/frontends/cmn/ecm_multicast_ipv4.c
+++ b/frontends/cmn/ecm_multicast_ipv4.c
@@ -1411,8 +1411,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 	prevalent_pr.process_actions = 0;
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_multicast_ipv6.c ecm-final/frontends/cmn/ecm_multicast_ipv6.c
--- a/frontends/cmn/ecm_multicast_ipv6.c
+++ b/frontends/cmn/ecm_multicast_ipv6.c
@@ -1373,8 +1373,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 	prevalent_pr.process_actions = 0;
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_non_ported_ipv4.c ecm-final/frontends/cmn/ecm_non_ported_ipv4.c
--- a/frontends/cmn/ecm_non_ported_ipv4.c
+++ b/frontends/cmn/ecm_non_ported_ipv4.c
@@ -717,8 +717,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_non_ported_ipv6.c ecm-final/frontends/cmn/ecm_non_ported_ipv6.c
--- a/frontends/cmn/ecm_non_ported_ipv6.c
+++ b/frontends/cmn/ecm_non_ported_ipv6.c
@@ -647,8 +647,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_ported_ipv4.c ecm-final/frontends/cmn/ecm_ported_ipv4.c
--- a/frontends/cmn/ecm_ported_ipv4.c
+++ b/frontends/cmn/ecm_ported_ipv4.c
@@ -1012,8 +1012,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 
diff -Nur '--exclude=.git' ecm-final-base/frontends/cmn/ecm_ported_ipv6.c ecm-final/frontends/cmn/ecm_ported_ipv6.c
--- a/frontends/cmn/ecm_ported_ipv6.c
+++ b/frontends/cmn/ecm_ported_ipv6.c
@@ -1035,8 +1035,8 @@
 	prevalent_pr.drop = false;
 	prevalent_pr.flow_qos_tag = skb->priority;
 	prevalent_pr.return_qos_tag = skb->priority;
-	prevalent_pr.flow_int_pri = skb->int_pri;
-	prevalent_pr.return_int_pri = skb->int_pri;
+	prevalent_pr.flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
+	prevalent_pr.return_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 	prevalent_pr.accel_mode = ECM_CLASSIFIER_ACCELERATION_MODE_ACCEL;
 	prevalent_pr.timer_group = ci_orig_timer_group = ecm_db_connection_timer_group_get(ci);
 
diff -Nur '--exclude=.git' ecm-final-base/frontends/ecm_front_end_common.c ecm-final/frontends/ecm_front_end_common.c
--- a/frontends/ecm_front_end_common.c
+++ b/frontends/ecm_front_end_common.c
@@ -762,7 +762,7 @@
 	if (dscpcte) {
 		if (sender == ECM_TRACKER_SENDER_TYPE_SRC) {
 			dscpcte->flow_priority = skb->priority;
-			dscpcte->flow_int_pri = skb->int_pri;
+			dscpcte->flow_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			dscpcte->flow_mark = skb->mark;
 			dscpcte->flow_dscp = iph->ds >> XT_DSCP_SHIFT;
 			dscpcte->flow_set_flags = NF_CT_DSCPREMARK_EXT_PRIO | NF_CT_DSCPREMARK_EXT_DSCP | NF_CT_DSCPREMARK_EXT_MARK;
@@ -770,7 +770,7 @@
 				    ct, sender, dscpcte->flow_priority, dscpcte->flow_int_pri, dscpcte->flow_dscp, dscpcte->flow_mark, dscpcte->flow_set_flags);
 		} else {
 			dscpcte->reply_priority =  skb->priority;
-			dscpcte->reply_int_pri = skb->int_pri;
+			dscpcte->reply_int_pri = 0;	/* skb->int_pri requires QSDK kernel patches */
 			dscpcte->reply_mark =  skb->mark;
 			dscpcte->reply_dscp = iph->ds >> XT_DSCP_SHIFT;
 			dscpcte->return_set_flags = NF_CT_DSCPREMARK_EXT_PRIO | NF_CT_DSCPREMARK_EXT_DSCP | NF_CT_DSCPREMARK_EXT_MARK;
